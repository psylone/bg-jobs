# SupervisedTaskExample

Mix project that implements background task with [`Task.Supervisor`](https://hexdocs.pm/elixir/Task.Supervisor.html).

## Run project

```
mix deps.get
mix run --no-halt
```

Endpoint will be available at http://127.0.0.1:4000.
